{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build":"npm install --prefix ./frontend && npm run build --prefix ./frontend && npm install && npm i dotenv && python -m pip install nltk ",
    "vercel-build": "npm install --prefix ./frontend && npm run build --prefix ./frontend && npm install && npm i dotenv "
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
